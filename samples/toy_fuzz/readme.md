# Toy Fuzzing Sample

This is the smallest end-to-end fuzzing sample in the artifact.

Usage:

```bash
bash run.sh /path/to/patched-qemu
```

It builds a tiny stdin target, records a seed trace, and runs a short fuzzing campaign.
