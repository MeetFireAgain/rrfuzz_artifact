# Toy Record/Replay Sample

This is the smallest RR-Fuzz smoke test in the artifact.

Usage:

```bash
bash run.sh /path/to/patched-qemu
```

It builds a tiny target program, records a trace, and replays it.
